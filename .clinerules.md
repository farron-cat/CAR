# C 语言多文件编程规范（适用于 STC8 / 嵌入式项目）

当生成或修改 C 语言代码时，必须严格遵守以下规则。

## 1. 工程结构

- 按功能划分模块，每个模块由 `.c` + `.h` 配对。
- 文件夹分层：
  - `APP/`：业务逻辑（如 main.c）
  - `BSP/`：板级驱动（如 bsp_led.c/h），统一以 `bsp_` 前缀命名
  - `MCU/`：芯片官方库（如 STC8G_H_xxx.h）
- `.h` 是“接口”，`.c` 是“实现”。

## 2. 头文件包含顺序（.c 文件）

按以下顺序包含头文件：

1. 当前模块自己的头文件（如 `#include "bsp_xxx.h"`）
2. 芯片官方库头文件（如 `#include "STC8G_H_xxx.h"`）
3. 其他依赖头文件（如 `#include <stdio.h>`）

**目的**：优先检查自身接口，避免隐式依赖。

## 3. 头文件自包含

- 每个 `.h` 必须包含自身依赖的头文件（如使用了 `u8`，必须 `#include "type_def.h"`），不能依赖调用者间接包含。

## 4. 命名规范

### 4.1 文件命名

- 全小写，单词用下划线分隔，例如 `bsp_led.c`、`bsp_uart.h`。
- 禁用大小写混用、中文或特殊字符。

### 4.2 函数命名

- 格式：`模块名_功能名`（如 `UART_Init()`、`LED_SetState()`）。
- 统一动词：`Init`、`Get`、`Set`、`Read`、`Write`、`Send`、`Receive`。
- 缩写全大写（如 `LED`、`ADC`），完整单词首字母大写（如 `Buzzer_Init`）。

### 4.3 变量命名

- **局部变量**：小驼峰，如 `adcValue`。
- **全局变量**：`g_` + 小驼峰，如 `g_adcValue`。
- **静态变量**：`s_` + 小驼峰，如 `s_bufferIndex`。
- **指针/数组**：按含义命名，如 `rxBuffer`、`keyBuffer[8]`。
- **循环/临时变量**：可简写，如 `i`、`j`、`temp`。
- **宏/常量/枚举成员**：全大写 + 下划线，如 `LED_ON`、`ADC_MAX_VALUE`。
- **结构体类型**：`模块名_TypeDef`，如 `LED_TypeDef`，成员按普通变量规则。

## 5. 注释规范

- 文件头、函数头使用 Doxygen 风格：

  ```c
  /**
   * @file    bsp_xxx.c
   * @brief   功能描述
   */
  /**
   * @brief  函数描述
   * @param  参数说明
   * @return 返回值说明
   */
